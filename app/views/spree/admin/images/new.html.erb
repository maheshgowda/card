<%= render partial: 'spree/admin/shared/product_tabs', locals: { current: :images } %>
<%= render partial: 'spree/admin/shared/greetingcard_tabs', locals: { current: :images } %>

<%= render partial: 'spree/admin/shared/error_messages', locals: { target: @product } %>
<%= render partial: 'spree/admin/shared/error_messages', locals: { target: @greetingcard } %>

<% content_for :page_title do %>
  <%= Spree.t(:new_image) %>
<% end %>

<%= form_for [:admin, @product, @image, @greetingcard], html: { multipart: true } do |f| %>
  <fieldset data-hook="new_image">
    <%= render partial: 'form', locals: { f: f } %>

    <div class="form-actions" data-hook="buttons">
      <%= button Spree.t('actions.create'), 'save' %>
      <span class="or"><%= Spree.t(:or) %></span>
      <%= button_link_to Spree.t('actions.cancel'), spree.admin_product_images_url(@product), icon: 'delete', id: 'cancel_link' %>
      <%= button_link_to Spree.t('actions.cancel'), spree.admin_greetingcard_images_url(@greetingcard), icon: 'delete', id: 'cancel_link' %>
    </div>
  </fieldset>
<% end %>
